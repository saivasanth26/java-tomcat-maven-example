
apiVersion: v1
kind: Service
metadata:
  labels:
    app: lab_tomcat
  name: lab_tomcat
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
  selector:
    app: lab_tomcat
